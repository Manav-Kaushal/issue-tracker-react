import { ReactNode } from "react";

type Props = { content: string | ReactNode };

const BroadcastStrip = ({ content }: Props) => {
  return <div>BroadcastStrip</div>;
};

export default BroadcastStrip;
